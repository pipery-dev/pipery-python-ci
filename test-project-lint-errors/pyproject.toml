[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hello-lint-errors"
version = "0.1.0"
requires-python = ">=3.8"

[tool.hatch.build.targets.wheel]
packages = ["src/hello_lint"]
