import os  # noqa: F401 -- but still an unused import ruff will flag
import sys
import json
import re
x=1  # E225: missing whitespace around operator
y = 2;z = 3  # E702: multiple statements on one line
def bad_function( a,b ):  # E201/E202: whitespace in brackets
    return a+b  # E225
